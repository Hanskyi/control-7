export interface IItems {
    id: string;
    name: string;
    price: number;
    image: string
}

export interface IBasked {
    id: string;
    name: string;
    price: number;
    count: number;
}